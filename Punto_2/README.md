## Correcciones


### Ejercicio nro 1

Cambios de nombres:
- A la variable dato secreto la cambie por numero, ya que se entiende mejor.
- Procedimiento Procesar_enigma, lo cambie por Procesar_numero, ya que procesara un numero X-X.
- f_alpha la cambie por f_invertir_num, esta funcion lo que hace es invertir el numero que es enviado por otra funcion por referencia.
- cambio de nombres de las variables en la funcion f_invertir_num: variable temp es cambiada por numero, y variable rev por inverso, ahora la variable numero guardara el numero, y inverso el numero invertido de numero.
- Cambio de nombre en el procedimiento f_beta por f_dividir, lo que hace esta funcion es dividir por 2 el numero enviado por la f_invertir_num, que seria el numero invertido.
- Cambio de nombre en el procedimiento f_gamma por f_sumar_resto, esta funcion lo que hace es sumar el resto del numero que va siendo divido por 10, hasta que sea 0.


### Ejercicio nro 2

Agregando cambios en el codigo de codigo_sin_funcionar uwu:
- Primero agrego la libreria stdio.h para que lea por pantalla y escriba.
- agrego el & al primer scanf para que agarre el valor.
- agrego ; en la variable suma
- agrego el valor por referencia en el preocedimiento duplicar_numero
- agregue ; en return